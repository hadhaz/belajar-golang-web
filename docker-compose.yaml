services:
  mysql:
    image: mysql
    environment:
      MYSQL_DATABASE: db_belajar_golang
      MYSQL_USER: learner
      MYSQL_PASSWORD: secret
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"
    restart: always
